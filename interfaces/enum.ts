export enum Gender {
  MALE = 'MALE',
  FEMALE = 'FEMALE'
}

export enum UserStatus {
  ACTIVE = 'ACTIVE',
  BANNED = 'BANNED',
  DELETED = 'DELETED'
}

export enum UserRole {
  DOCTOR = 'DOCTOR',
  PATIENT = 'PATIENT',
  ADMIN = 'ADMIN',
  SUPER_ADMIN = 'SUPER_ADMIN'
}

export enum AppointmentStatus {
   PENDING = "PENDING",
    SCHEDULED="SCHEDULED",
    COMPLETED="COMPLETED",
    CANCELLED="CANCELLED",
}